const Loader = () => (
  <div className="fixed inset-0 flex items-center justify-center bg-black/30 bg-opacity-50 z-50">
    <div className="loader-spinner border-4 border-t-4 border-gray-200 rounded-full w-12 h-12 animate-spin"></div>
  </div>
);

export default Loader;
